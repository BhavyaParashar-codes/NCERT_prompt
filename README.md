# NCERT_prompt
This project uses Python and OpenAI (DeepSeek) to extract structured content from NCERT PDFs. It converts extracted data into JSON and Excel formats. A dynamic prompt then generates a custom day-wise study planner from the Excel, based on user-defined duration.
